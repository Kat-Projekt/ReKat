/* mainfesto
this is a separate program that runs insde the main one 

it splits every feater of the program into it's own instance, 
it runs in the same thread ( different is optional )

this gives the ability to execute foreign code inside the egnie with out ever closing the engine
a context can be started dueirng the execution as an objekt and a different program can be atached to it.
*/