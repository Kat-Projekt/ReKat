#ifndef LOGIK_MANAGER
#define LOGIK_MANAGER

/*
#include "../../resource.hpp"

#include "animation.h"

namespace Manager {
    static Animation < int >* Animation_Get  ( std::string name )
	{ return Get < Animation < int > > ( name ); }
    template < typename T >
    static int Animation_Load ( std::string name, T* parameter, PlayMode play = ONCE ) {
        auto s = new Animation < T >;
        _resources.insert( { (name + std::string(typeid(Animation < int >).name())) , s } );
        return (*s).Make( parameter, play );
    }
    template < typename T >
    static int Animation_Load ( std::string name, Animation < T > &anim ) {
        _resources.insert( { (name + std::string(typeid(Animation < int >).name())) , anim } );
    }
}; // namespace Manager
*/

#endif